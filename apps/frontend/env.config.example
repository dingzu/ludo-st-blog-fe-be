# 环境配置示例文件
# 
# 使用说明：
# 1. 在 apps/frontend 目录下创建 .env.local 文件
# 2. 复制下面的配置到 .env.local
# 3. 根据你的需求修改配置

# ============================================================
# 开发环境配置（.env.local）
# ============================================================

# ---------- API 模式选择 ----------
# mock - 使用本地 Mock 数据（适合前端独立开发）
# api - 连接真实后端服务
VITE_API_MODE=api

# ---------- 后端 API 地址 ----------
# 根据你的实际情况选择一个配置

# 方案1：本地后端服务
VITE_API_BASE_URL=http://localhost:3000/api

# 方案2：远程测试环境
# VITE_API_BASE_URL=https://test-api.example.com/api

# 方案3：生产环境（谨慎使用）
# VITE_API_BASE_URL=https://api.example.com/api

# 方案4：Railway 部署的后端
# VITE_API_BASE_URL=https://your-app.railway.app/api

# ---------- 代理配置 ----------
# true - 启用代理（推荐，自动处理跨域）
# false - 不使用代理（需要后端配置 CORS）
VITE_USE_PROXY=true

# ---------- 其他配置 ----------
# 请求超时时间（毫秒）
VITE_API_TIMEOUT=30000

# 是否启用请求日志
VITE_ENABLE_API_LOG=true

