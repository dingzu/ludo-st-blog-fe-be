{
  "name": "@ludost/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Ludost Blog",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.1.0"
  }
}
